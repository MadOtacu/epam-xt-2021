function DoubleDestroyer(){
    var inputString = "у попа?была   собака"
    inputString.split(' ').join(',').split('?').join(',').split('!').join(',').split(':').join(',').split(';').join(',').split('.').join(',').split(',')
    console.log(inputString)
}